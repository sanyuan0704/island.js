{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "vendors/**"]
    },
    "dev": {
      "cache": false
    },
    "//#build:docs": {
      "dependsOn": ["^build"],
      "outputs": ["docs/.island/dist/**"]
    }
  }
}
