import * as $0 from "./routes/index";
import * as $$0 from "./islands/Counter";

const manifest = {
  routes: {
    "/": $0,
  },
  islands: {
    "/islands/Counter": $$0,
  },
  baseUrl: import.meta.url,
};

export default manifest;
